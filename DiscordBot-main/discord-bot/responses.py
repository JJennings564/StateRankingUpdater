
def get_response(message: str) -> str:
    p_message = message.lower()

def set_target(message: str) -> str:
 if user_message
 target: str = message.lower()

 def command_interaction(interaction: str) -> str:
    p_interaction = interaction.lower